# Credentials - never commit
creds.txt
*.tfvars
!terraform.tfvars.example

# Generated inputs for the IACM stage. Must be committed: the pipeline reads
# it from the repository. Contains no secrets by construction.
!iacm/config.auto.tfvars

# OpenTofu / Terraform state and local cache
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# Build artifacts
.build/
*.zip

# OS
.DS_Store
