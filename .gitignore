# Credentials - never commit
creds.txt
*.tfvars
!terraform.tfvars.example

# Non-secret project definitions for the self-service workspace flow (see
# iacm/variables.tf). Must be committed: pushing one is what creates a
# project's workspaces and deployments.
!environments/*.tfvars

# OpenTofu / Terraform state and local cache
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# Build artifacts
.build/
*.zip

# OS
.DS_Store

# Saved plan files
tfplan
modules/lambda/.placeholder/
